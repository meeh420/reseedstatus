Mysql schema for database is in config.php-dist,
rename config.php-dist to config.php

make sure the tmp folder is set to read & write for user running check scripts.

set ridate/ripubd.py to executable.

